{
  "name": "job-search-studio",
  "version": "0.1.0",
  "private": true,
  "description": "Full-stack TypeScript workspace for Job Search Studio.",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "engines": {
    "node": ">=18.18.0"
  },
  "devDependencies": {
    "prettier": "^3.9.4"
  }
}
